using NUnit.Framework;
using UnityEngine;

public class Dialogue_Manager_UI : MonoBehaviour
{
    //essential the director of the scripts, and diplsya UIin silenc
    string[] lines = null; //all the lines in the dialogue, defined by the length of the array of strings

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {

    }

    //opens the panel, freezes the player and world, starts the dialogue, and sets the lines to the lines of the dialogue
    //ENTERS CUTSCENE MODE
    void StartConversation()
    {

    }

    //closes the panel, unfreezes the player and world, and ends the dialogue
    void EndConversation()
    {
    }

    //displays the next line of dialogue, if there are no more lines then it ends the conversation
    void displayNextLine()
    {

    }

    //shows choices
    void diplayChoices()
    {
    }

    //calls the action to be done
    void SelectChoice(int choice)
    {
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
